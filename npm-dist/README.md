
## Dependencies

The package will automatically attempt to install `@ast-grep/cli` as an optional dependency.
If ast-grep is not found in your PATH after installation, AST code search features will not work.

Install manually if needed:

```bash
npm install -g @ast-grep/cli
# or
bun add -g @ast-grep/cli
```
